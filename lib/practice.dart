import 'package:flutter/foundation.dart';

void main() {
  // Avoid printing in production; only print in debug mode.
  if (kDebugMode) {
    // ignore: avoid_print
    print('This is a practice file.');
  }
}
